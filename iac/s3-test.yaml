apiVersion: s3.aws.upbound.io/v1beta2
kind: Bucket
metadata:
  name: test-s3-bucket-112
  namespace: gitops
spec:
  forProvider:
    region: us-east-1
    acl: private
    versioning:
      enabled: true
    tags:
      Environment: test
      Purpose: crossplane-demo
  providerConfigRef:
    name: default
